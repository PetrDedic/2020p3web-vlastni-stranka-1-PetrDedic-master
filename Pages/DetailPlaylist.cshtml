@page
@model P3DedicApp.DetailPlaylistModel

@{
    ViewData["Title"] = "DetailPlaylist";
}

<h1>DetailPlaylist</h1>

<div>
    <h4>Playlist</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Playlist.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Playlist.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Playlist.User)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Playlist.User.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Playlist.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
